# Configuration for SonarQube Cloud Automatic Analysis.
# Scope: library code plus the CI workflows (supply-chain checks); tests are analyzed under test rules,
# and examples/ and the test-only keys in assets/ stay out of analysis entirely.
sonar.sources=src,.github
sonar.tests=tests
sonar.sourceEncoding=UTF-8
